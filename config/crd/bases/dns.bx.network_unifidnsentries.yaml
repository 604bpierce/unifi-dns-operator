---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: unifidnsentries.dns.bx.network
spec:
  group: dns.bx.network
  names:
    kind: UnifiDNSEntry
    listKind: UnifiDNSEntryList
    plural: unifidnsentries
    singular: unifidnsentry
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UnifiDNSEntry is the Schema for the unifidnsentries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of UnifiDNSEntry
            properties:
              domain:
                description: Domain is the domain name for the DNS entry
                minLength: 1
                type: string
              enabled:
                default: true
                description: Enabled determines if the DNS policy is active
                type: boolean
              recordData:
                description: RecordData contains type-specific DNS record information
                properties:
                  ipv4Address:
                    description: IPv4Address is the IPv4 address for A_RECORD
                    type: string
                  ipv6Address:
                    description: IPv6Address is the IPv6 address for AAAA_RECORD
                    type: string
                  mailServerDomain:
                    description: MailServerDomain is the mail server domain for MX_RECORD
                    type: string
                  port:
                    description: Port is the port number for SRV_RECORD
                    type: integer
                  priority:
                    description: Priority is the priority for MX_RECORD and SRV_RECORD
                    type: integer
                  target:
                    description: Target is the target server for SRV_RECORD
                    type: string
                  targetDomain:
                    description: TargetDomain is the target domain for CNAME_RECORD
                    type: string
                  textValue:
                    description: TextValue is the text value for TXT_RECORD
                    type: string
                  weight:
                    description: Weight is the weight for SRV_RECORD
                    type: integer
                type: object
              ttlSeconds:
                description: TTLSeconds is the Time-To-Live for the DNS record in
                  seconds
                type: integer
              type:
                description: Type specifies the DNS record type
                enum:
                - A_RECORD
                - AAAA_RECORD
                - CNAME_RECORD
                - MX_RECORD
                - TXT_RECORD
                - SRV_RECORD
                type: string
            required:
            - domain
            - enabled
            - recordData
            - type
            type: object
          status:
            description: status defines the observed state of UnifiDNSEntry
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the UnifiDNSEntry resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              policyId:
                description: PolicyID is the UUID of the DNS policy in Unifi
                type: string
              synced:
                description: Synced indicates whether the DNS entry is synchronized
                  with Unifi
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
